---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: home-assistant
  namespace: home
  labels:
    app: home-assistant
spec:
  replicas: 1
  selector:
    matchLabels:
      app: home-assistant
  template:
    metadata:
      labels:
        app: home-assistant
      annotations:
        # Attach to IoT VLAN for mDNS/SSDP device discovery
        # Static IP 10.0.30.150 via static IPAM
        k8s.v1.cni.cncf.io/networks: |
          [{
            "name": "macvlan-iot",
            "namespace": "default",
            "ips": ["10.0.30.150/24"]
            "default-route": ["10.0.30.1"]
          }]
    spec:
      hostname: home-assistant
      containers:
      - name: home-assistant
        image: ghcr.io/home-assistant/home-assistant:2026.6.4
        env:
        - name: TZ
          value: "Asia/Manila"
        ports:
        - containerPort: 8123
          name: http
          protocol: TCP
        volumeMounts:
        - name: config
          mountPath: /config
        securityContext:
          privileged: true
      volumes:
      - name: config
        persistentVolumeClaim:
          claimName: home-assistant-pvc
      restartPolicy: Always
      # Pin to xialing (only node with eno1.300 VLAN interface configured)
      nodeName: xialing
