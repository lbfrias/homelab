{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":dependencyDashboard",
    ":semanticCommits"
  ],
  "flux": {
    "fileMatch": ["(^|/)manifests/.+\\.yaml$"]
  },
  "helm-values": {
    "fileMatch": ["(^|/)manifests/.+\\.yaml$"]
  },
  "kubernetes": {
    "fileMatch": ["(^|/)manifests/.+\\.yaml$"]
  },
  "packageRules": [
    {
      "description": "Leaf-level grouping for apps (each app directory = one PR)",
      "matchPaths": ["manifests/apps/**"],
      "groupName": "{{parentDir}}"
    },
    {
      "description": "Schedule infrastructure updates for Monday morning",
      "matchPaths": ["manifests/infrastructure/**"],
      "schedule": ["before 9am on monday"]
    },
    {
      "description": "Automerge patch updates for Helm charts",
      "matchDatasources": ["helm"],
      "matchUpdateTypes": ["patch"],
      "automerge": true
    }
  ],
  "ignorePaths": [
    "**/ansible/**",
    "**/docs/**"
  ]
}
