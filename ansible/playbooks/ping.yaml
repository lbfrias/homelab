---
# Verify connectivity to all cluster nodes
# Usage (from ansible/ dir): ansible-playbook -e @vars.local.yaml playbooks/ping.yaml

- name: Ping all nodes
  hosts: all
  gather_facts: false
  tasks:
    - name: Ping
      ansible.builtin.ping:
