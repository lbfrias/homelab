---
all:
  vars:
    ansible_python_interpreter: auto_silent
    # ansible_user and ansible_port loaded via -e @vars.local.yaml

  children:
    # Raspberry Pi nodes
    rpi:
      hosts:
        peggy:
        yelena:

    # x86 nodes
    x86:
      hosts:
        xialing:

    # K3s cluster (all nodes)
    k3s_cluster:
      children:
        rpi:
        x86:
